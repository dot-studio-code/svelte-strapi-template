# Enforce node version from package.json
engine-strict=true

# Save exact versions for dependencies
save-exact=true

# Always use package-lock.json
package-lock=true

# Optional: use public registry (change if using private registry)
registry=https://registry.npmjs.org/

# Optional: log level (silent, error, warn, http, info, verbose)
loglevel=warn
